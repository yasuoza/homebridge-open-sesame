{
  "private": true,
  "description": "This file keeps track of which plugins should be installed.",
  "scripts": {
    "start": "homebridge --no-qrcode --debug --user-storage-path=./"
  },
  "dependencies": {
    "homebridge": "1.3.4",
    "homebridge-open-sesame": "./homebridge-open-sesame"
  }
}
